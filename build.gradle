plugins {
    id 'java'
}

subprojects {
    group 'codes.reason'
    version '1.0-SNAPSHOT'

    apply plugin: 'java'

    repositories {
        mavenCentral()
        maven {
            url 'https://repo.hypixel.net/repository/Hypixel/'
        }
    }
}